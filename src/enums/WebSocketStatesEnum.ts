
export enum WebSocketState {
    OPEN = 'OPEN',
    CLOSED = 'CLOSED',
    ERROR = 'ERROR',
    MAKE_MOVE = 'MAKE_MOVE',
    UPDATE = "UPDATE",
    JOIN = "JOIN",
    JOIN_RESPONSE = 'JOIN_RESPONSE'
}
